#include<stdio.h>
#include<conio.h>
int main()
{
    int r,a;
    printf("Enter the Radius of the Circle");
    scanf("%d", &r);
    a=3.14*r*r;
    printf("The Area of the Circle is= %d",a);
    return 0;
}